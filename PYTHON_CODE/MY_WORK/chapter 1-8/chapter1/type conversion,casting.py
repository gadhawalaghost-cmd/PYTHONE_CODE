          # type conversion
      # we cannot put string values
a,b=1,2.0 
sum=(a+b)
print(sum)

        # type casting
      # we can  put string values(using int type)

a,b=1,"2"
c=int(b)
sum=a+c
print(sum)
